[project]
name = "call666"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "llm-sdk",
    "pydantic>=2.13.4",
    "torch>=2.12.0",
    "transformers>=5.12.1",
]

[tool.uv.sources]
llm-sdk = { path = "llm_sdk" }
